# 移除历史数据获取数量限制 - 更新日志

## 更新时间
2026-03-24

## 问题背景
用户反馈："为什么要加数量限制呢？这样不就漏了吗？"

原策略中，即使初步筛选出100只候选股票，也只获取前50只的历史数据，可能导致优质股票被遗漏。

## 解决方案

### 1. 代码修改

**src/index.js - enrichWithHistory() 方法**

移除前：
```javascript
const maxFetch = this.config.strategy.maxHistoryFetch || 50;
if (!history && fetchCount < maxFetch) {
  // 获取历史数据
}
```

移除后：
```javascript
// 不再限制获取数量
if (!history) {
  // 获取所有候选股票的历史数据
}
```

### 2. 配置文件更新

**config/default.json**

移除了 `maxHistoryFetch` 参数：
```json
{
  "strategy": {
    "topN": 30,
    "enableHistoryScore": true
    // 移除: "maxHistoryFetch": 50
  }
}
```

### 3. 文档更新

**STRATEGY_60DAY_FINAL.md**

更新了第三步描述：
- 旧：最多获取50只股票的历史数据
- 新：获取所有候选股票的历史数据（无数量限制）

## 实际效果

### 测试结果（2026-03-24 12:36:34）

```
初步筛选：11只候选股票（评分≥70分）
历史数据获取：11/11 全部获取
60日严格筛选：0只通过

被过滤原因：
- 10只：60日涨幅过大（>80%，追高风险）
- 1只：60日涨幅为负（-19.4%）
- 1只：最大回撤过大（75.89%）
```

### 性能影响

- **获取时间**：约2秒（11只股票）
- **缓存机制**：1小时有效期，避免重复请求
- **请求间隔**：100ms，避免API限流

## 优势

1. **不遗漏优质股票**：所有候选股票都会被分析
2. **更准确的筛选**：基于完整的候选池进行60日历史过滤
3. **灵活性更高**：候选数量可以根据市场情况动态调整

## 注意事项

1. 如果候选股票数量过多（>100只），获取时间会相应增加
2. 建议保持初步筛选标准（70分），避免候选池过大
3. 缓存机制确保性能，1小时内重复扫描不会重复获取

## 总结

移除数量限制后，策略更加完整和准确，不会因为人为限制而错过优质股票。配合严格的60日历史过滤条件，确保选出的都是真正有持续上涨能力的标的。

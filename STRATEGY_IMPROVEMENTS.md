# 交易策略优化说明

## 修改时间：2026-03-24

## 核心问题修复

### 问题1：账户状态不持久化 ✅
**问题**：每次重启系统都是1000万初始资金，之前的交易记录和持仓全部丢失
**解决**：
- 在PaperAccount构造函数中，首次启动时保存初始状态
- 重启时从orders.json加载现金、持仓、订单、冷却期数据
- 所有交易操作后立即调用saveState()保存状态

### 问题2：扫描间隔不合理 ✅
**问题**：非交易时间也频繁扫描，浪费资源
**解决**：
- 交易时间：3分钟扫描一次（180秒）
- 非交易时间：10分钟扫描一次（600秒）
- 动态调整：每次扫描后根据当前时间判断下次间隔
- 使用setTimeout替代setInterval，实现动态间隔

## 主要问题及解决方案

### 1. 时间显示问题 ✅
**问题**：所有时间显示为UTC时间，不符合中国市场习惯
**解决**：
- 新增 `toBeijingTime()` 和 `formatBeijingTime()` 函数
- 所有日志和交易记录显示北京时间（UTC+8）
- 交易时间判断基于北京时间

### 2. 频繁交易同一只股票 ✅
**问题**：同一只股票在几分钟内反复买卖，产生大量手续费损耗
**解决**：
- 新增 `sellCooldown` Map记录每只股票的卖出时间
- 新增配置 `buyCooldownMinutes: 30`（默认30分钟冷却期）
- 卖出后30分钟内不能再次买入同一只股票

### 3. 持有时间过短 ✅
**问题**：`maxHoldScanRounds=3` 意味着最多持有3分钟就卖出
**解决**：
- 调整为 `maxHoldScanRounds: 20`
- 扫描间隔从60秒改为300秒（5分钟）
- 实际最长持有时间：20轮 × 5分钟 = 100分钟

### 4. 止盈止损阈值不合理 ✅
**问题**：
- 止盈6%太保守，错失更大收益
- 止损-3%太激进，正常波动就触发
- 回撤2.5%过于敏感

**解决**：
- 止盈：6% → 8%
- 止损：-3% → -4%
- 回撤阈值：2.5% → 4%
- 评分退出阈值：65 → 60（更宽容）

### 5. 回撤计算方式错误 ✅
**问题**：使用当日最高价计算回撤，而非持仓期间最高价
**解决**：
- 新增 `highPrice` 字段记录持仓期间最高价
- 每轮更新时追踪最高价
- 从持仓最高点计算回撤，更合理

### 6. T+1和交易时间限制 ✅
**已实现**：
- T+1：当天买入不能当天卖出
- 交易时间：周一至周五 9:30-11:30, 13:00-15:00
- 非交易时间只更新权益，不执行交易

## 配置参数对比

| 参数 | 修改前 | 修改后 | 说明 |
|------|--------|--------|------|
| scanIntervalMs | 60000 (1分钟) | 180000 (3分钟) | 交易时间扫描间隔 |
| scanIntervalOffHoursMs | - | 600000 (10分钟) | 非交易时间扫描间隔 |
| takeProfitPct | 6% | 8% | 提高止盈目标 |
| stopLossPct | -3% | -4% | 放宽止损空间 |
| exitScoreThreshold | 65 | 60 | 降低评分退出阈值 |
| exitDrawdownFromHighPct | 2.5% | 4% | 放宽回撤容忍度 |
| maxHoldScanRounds | 3 | 20 | 延长持有时间 |
| buyCooldownMinutes | - | 30 | 新增冷却期 |

## 实际持有时间计算

- 交易时间：20轮 × 3分钟 = 60分钟
- 非交易时间不计入holdRounds
- 最长持有时间约1小时（交易时段内）

## 状态持久化机制

**保存内容**：
- cash：当前现金
- positions：持仓列表（Map转Array）
- orders：最近1000条订单
- sellCooldown：卖出冷却期记录

**保存时机**：
- 首次启动时
- 每次买入/卖出后
- 系统重启时自动加载

**文件位置**：
- `/logs/orders.json` - 账户状态
- `/logs/trades.log` - 交易流水（追加模式）
- `/logs/equity.log` - 权益曲线（追加模式）

## 预期效果

1. **减少交易成本**：冷却期避免频繁交易同一只股票
2. **提高持仓稳定性**：更长的持有时间和更宽松的退出条件
3. **优化收益风险比**：8%止盈/-4%止损，风险收益比2:1
4. **降低误触发**：4%回撤阈值避免正常波动导致卖出
5. **符合监管要求**：T+1和交易时间限制

## 监控建议

1. 观察冷却期是否过长，可能错失机会
2. 关注持仓时间分布，是否大部分持有到maxHoldScanRounds
3. 统计止盈止损触发比例，优化阈值
4. 监控回撤触发频率，判断4%是否合适
